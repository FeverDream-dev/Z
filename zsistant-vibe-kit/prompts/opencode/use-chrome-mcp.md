# Prompt: Use Chrome DevTools MCP for UI validation

```text
Use Chrome DevTools MCP only if configured. Open the local Zsistant web UI. Validate the dashboard, agent list, and local chat flow for the current phase. Inspect console and network errors. Do not automate logged-in personal accounts. Report findings and exact reproduction steps.
```
